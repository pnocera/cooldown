**Goal** : 
create a local first reverse proxy / rate limiting server to reverse proxy and apply rate limiting to outgoing requests from specified urls. multiplatform, fast and efficient. the rate limiter would not block request, just add a delay to them, to avoid 409 errors.

**Tech Stack**:
Golang version 25.3

